# OS Junk
.DS_Store
*.log

# Python
__pycache__/
*.py[cod]
venv/
.env
.gemini/

# Piper TTS (local venv + downloaded voice models, ~160MB)
.piper-venv/
.piper-voices/

# Python AI backend — persisted vector index (large, regenerated on startup)
backend/storage/

# Node / JS
node_modules/
.next/
dist/
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Backups and Bloat
*.tar.gz
*.docx
*.bak

# Company formation docs — contain personal data (SSN, address). Keep out of git.
formation/
# Book build snapshots (local only)
book-archive/
Book Latest Version/
