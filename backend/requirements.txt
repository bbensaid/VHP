# HTR AI Brain — Python dependencies
# Install: pip install -r requirements.txt
# Versions pinned 2026-03-18 — bump deliberately, test after each upgrade.

# Web framework
fastapi~=0.127.0
uvicorn[standard]~=0.40.0

# Config
python-dotenv~=1.2.0
pydantic~=2.12.0

# LlamaIndex core + integrations
llama-index-core~=0.14.10
llama-index-llms-groq~=0.3.0
llama-index-llms-anthropic~=0.6.0
llama-index-embeddings-openai~=0.3.0
llama-index-vector-stores-postgres~=0.4.0
asyncpg~=0.30.0
llama-index-readers-file~=0.5.5


# PDF parsing (required by SimpleDirectoryReader for .pdf files)
pypdf~=6.4.0

# HTTP client (for Sanity GROQ API calls)
httpx~=0.28.0

# Supabase Python client (for JWT verification and DB access)
supabase~=2.15.0

# JWT verification for Supabase auth tokens
PyJWT~=2.10.0
cryptography~=46.0.5

# Re-ranking (cross-encoder, no GPU required)
flashrank~=0.2.9

# Error monitoring
sentry-sdk[fastapi]~=2.0

# Rate limiting
slowapi~=0.1.9

