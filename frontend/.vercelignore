# Exclude Sanity Studio (separate project, deploys via `sanity deploy`)
# Leading slash anchors to the project root so it won't match app/api/webhooks/sanity/
/sanity/

# Exclude local Next.js build artifacts
.next/

# Exclude test/debug files
*.test.*
